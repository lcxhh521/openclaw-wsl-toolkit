# Agent Collaboration v0

This module captures the first OpenClaw multi-agent collaboration pattern used between Telegram-facing `main` and an external coding/Codex agent.

## Goals

- Keep Telegram/main responsive.
- Let agents exchange structured, asynchronous messages without pasting full text through the user.
- Preserve Alex as the owner/approval gate.
- Prefer read-only diagnostics and additive artifacts.
- Avoid high-frequency polling and heavy gateway/status probes.

## Mailbox protocol

A mailbox directory contains:

- `codex_to_main.md` — external agent to OpenClaw main.
- `main_to_codex.md` — OpenClaw main to external agent.
- `turn.json` — turn state: `seq`, `last_writer`, `needs_reply`, `updated_at`, paths, and note.

The reliable completion signal is not “a watcher process was triggered”; it is `turn.json` being advanced by the expected writer.

## Scripts

- `scripts/openclaw-main-mailbox-watch.py`
  - Conservative watcher for `needs_reply=main` turns.
  - Uses lock file, per-seq retry cooldown, and max attempts.
  - Does not delete files.

- `scripts/daily_status_card.py`
  - Read-only daily delivery status card from local manifests/tasks/checkpoints.
  - Intended to answer “what succeeded, what is blocked, and what is the next safe action?” without publishing.

- `scripts/main_session_status.py`
  - Read-only main session model/context/fallback visibility from local `sessions.json`.
  - Does not call gateway/model/network.

## Safety boundaries

- Do not delete user files, especially Windows/OneDrive/user document paths.
- For files clearly generated by this module inside WSL workspace, prefer overwrite/update over deletion.
- Do not read secrets, tokens, cookies, private keys, or credential files.
- Do not run high-frequency status polling.
- Do not make external writes (Telegram/Notion/GitHub/etc.) from automation without explicit approval.
- Quality/content changes are discussion-only until Alex approves.

## Current limitations

- This is v0, not a complete agent room system.
- Codex-side wakeup still depends on Codex-side heartbeat/automation.
- Status scripts are diagnostic surfaces; they do not guarantee successful delivery by themselves.
- Recovery actions must be routed through background tasks with preflight and quality gates.
