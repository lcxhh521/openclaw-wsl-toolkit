# Approval-Gated Send Dry-Run Fixtures

These fixtures are local Agent Room validation artifacts only.
They do not send Telegram, activate production behavior, edit OpenClaw source,
advance canonical tasks, publish, or notify.

Run:

```bash
python3 /home/lcxhh/.openclaw/workspace/codex-main-bridge/agent-room/tools/approval_gated_send_validator.py
```
